import 'package:flutter_test/flutter_test.dart';

import 'package:in_app_notifier/in_app_notifier.dart';

void main() {
  test('adds one to input values', () {
    final calculator = InAppNotifier();
  });
}
